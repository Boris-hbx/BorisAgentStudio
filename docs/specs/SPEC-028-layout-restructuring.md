# SPEC-028: 布局重构 - SessionBar 整合

> 作者: Boris Huai
> 起草日期: 2026-01-27
> 状态: 已完成

## 概述

将 Session 选择功能从左侧 AgentExplorer 模块移至顶部 SessionBar，整合 StatusBar 功能，解决布局问题。

## 问题陈述

用户反馈（基于截图分析）：

1. **左侧红框**：Session 列表在侧边栏占用空间过多
2. **上侧红框**：StatusBar 仅显示 Session 标题，空间利用率低
3. **中间区域**：两侧栏展开时标题文字换行，布局不美观

## 解决方案

### 1. 新建 SessionBar 组件

替代原 StatusBar，整合以下功能：

- **Session 选择器**：下拉菜单选择 Session
- **搜索功能**：在下拉菜单内搜索 Session
- **导入功能**：导入外部 Session JSON
- **Session 信息**：显示当前标题、模型、状态
- **统计指标**：工具调用数、执行时长

### 2. 重构 AgentExplorer

从 Session 列表浏览器改为 Session 详情展示：

- **Session 概览**：状态、工具调用数、时长、模式
- **团队成员**：多 Agent 模式下显示角色统计
- **工具类别**：按类别统计工具调用
- **任务描述**：显示完整任务信息
- **用户提示**：显示原始用户输入

### 3. 更新 Timeline

移除 header 区域（标题、模型信息已在 SessionBar 展示）。

## 文件变更

### 新建文件

| 文件 | 描述 |
|------|------|
| `frontend/src/components/Layout/SessionBar.tsx` | SessionBar 组件 |
| `frontend/src/components/Layout/SessionBar.css` | SessionBar 样式 |

### 修改文件

| 文件 | 变更 |
|------|------|
| `frontend/src/components/Layout/index.ts` | 导出 SessionBar |
| `frontend/src/App.tsx` | 使用 SessionBar 替换 StatusBar |
| `frontend/src/components/Timeline/Timeline.tsx` | 移除 header 区域 |
| `frontend/src/components/Timeline/Timeline.css` | 移除 header 样式 |
| `frontend/src/components/Sidebar/modules/AgentExplorer/AgentExplorer.tsx` | 重构为详情展示 |
| `frontend/src/components/Sidebar/modules/AgentExplorer/AgentExplorer.css` | 更新样式 |

## 布局结构

```
┌─────────────────────────────────────────────────────────────┐
│                         Header                              │
├─────────────────────────────────────────────────────────────┤
│ [📋 Session ▼] │ Session Title          model │ ✓ 9 1.3min │ <- SessionBar
├────────┬────────────────────────────────────────────┬───────┤
│        │                                            │       │
│ Agent  │              Timeline                      │ Tool  │
│ Details│         (RoleActivityBar)                  │ Panel │
│        │         (PhaseGroupList)                   │       │
│        │                                            │       │
└────────┴────────────────────────────────────────────┴───────┘
```

## SessionBar 设计

```
┌──────────────────────────────────────────────────────────────┐
│ [📋 选择 Session ▼] │ Session标题    model │ ✓成功  工具:9  时长:1.3min │
└──────────────────────────────────────────────────────────────┘
                      ↓
┌─────────────────────────┐
│ [🔍 搜索...]      [📥] │
├─────────────────────────┤
│ ● Session 1             │
│ ● Session 2   ← 选中    │
│ ● Session 3             │
└─────────────────────────┘
```

## AgentExplorer 新设计

```
┌─────────────────────────┐
│ SESSION 概览            │
│ ┌─────────┬───────────┐ │
│ │状态:成功│工具调用:9 │ │
│ ├─────────┼───────────┤ │
│ │时长:1.3m│模式:多Agent│ │
│ └─────────┴───────────┘ │
├─────────────────────────┤
│ 团队成员                │
│ 👔 PO         3次调用   │
│ 🏛️ Architect  2次调用   │
│ 👨‍💻 Developer  4次调用   │
├─────────────────────────┤
│ 工具类别                │
│ 👁️ 感知              3  │
│ ⚡ 行动              5  │
│ 📋 规划              1  │
├─────────────────────────┤
│ 任务描述                │
│ 增强多Agent协作可视化...│
├─────────────────────────┤
│ 用户提示                │
│ /team 看我截图...       │
└─────────────────────────┘
```

## 响应式设计

SessionBar 响应式断点：

- `< 900px`：隐藏模型标签
- `< 1200px`：隐藏统计标签，仅显示数值

标题区域使用 `flex: 1; min-width: 0; overflow: hidden; text-overflow: ellipsis` 防止换行。

## 验证

- TypeScript 编译通过
- Vite 构建成功
- 布局响应式正常
- Session 选择和切换功能正常
