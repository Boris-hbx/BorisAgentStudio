# SPEC-032: AI 洞察模块

> 作者: Boris Huai
> 起草日期: 2026-01-28
> 状态: 实施中

## 1. 问题陈述 (Why)

Boris 需要系统化追踪 AI 行业动态，特别是：
- AI for Coding / Code Agent / Agentic Security 领域
- GitHub 上快速增长的相关项目

当前痛点：
1. 信息分散在多个来源，手动追踪低效
2. 缺少结构化积累
3. 无可视化呈现

## 2. 目标与成功标准 (DoD)

### MVP 目标
1. 左侧栏新增「洞察」模块（图标：🔭）
2. 主区域展示洞察内容：
   - 按主题分类（MVP 仅 Agent Security）
   - GitHub 热门项目（7天增长排序）
   - 行业新闻动态
3. `/insight` 命令触发每日洞察
4. 数据持久化至 `data/insights/`

### 成功标准
- [ ] 点击「洞察」图标切换到洞察视图
- [ ] 展示 Agent Security 主题的 GitHub 热门项目
- [ ] 展示相关行业新闻
- [ ] `/insight` 命令可执行并更新数据
- [ ] 架构支持扩展更多主题

## 3. 非目标 (Out of Scope)

- 自动定时执行（需用户手动触发）
- AI Coding、Code Agent 等其他主题（后续迭代）
- 复杂数据分析/趋势图表
- 用户自定义主题配置

## 4. 技术方案 (How)

### 4.1 数据结构

**主题配置**：`data/insights/topics.json`
```json
{
  "topics": [
    {
      "id": "agent-security",
      "name": "Agent 安全",
      "icon": "🛡️",
      "enabled": true,
      "keywords": ["agent security", "llm security", "ai safety"],
      "github_query": "topic:agent-security OR topic:llm-security stars:>50"
    }
  ]
}
```

**洞察数据**：`data/insights/{topic}/latest.json`
```typescript
interface InsightData {
  topic: string
  generated_at: string
  github_trending: GitHubProject[]
  news: NewsItem[]
}

interface GitHubProject {
  name: string              // 'owner/repo'
  description: string
  url: string
  stars: number
  stars_growth_7d: number
  forks: number
  language: string
  topics: string[]
  updated_at: string
}

interface NewsItem {
  title: string
  source: string
  url: string
  published_at: string
  summary?: string
  is_trusted?: boolean      // 权威来源标记
}
```

### 4.2 组件架构

```
frontend/src/
├── components/
│   ├── Sidebar/modules/
│   │   └── InsightNav/           # 左侧栏占位（预留）
│   └── Insight/                  # 洞察主视图
│       ├── InsightView.tsx       # 主容器
│       ├── InsightView.css
│       ├── TopicTabs.tsx         # 主题标签
│       ├── GitHubTrending.tsx    # GitHub 项目列表
│       ├── NewsFeed.tsx          # 新闻列表
│       ├── ProjectCard.tsx       # 项目卡片
│       ├── EmptyState.tsx        # 空状态
│       └── index.ts
├── types/
│   └── insight.ts
└── services/
    └── insightService.ts
```

### 4.3 数据加载

使用 Vite `import.meta.glob` 加载：
```typescript
const insightModules = import.meta.glob<{ default: InsightData }>(
  '../../../data/insights/*/latest.json',
  { eager: true }
)
```

### 4.4 `/insight` 命令

位置：`.claude/commands/insight.md`

执行流程：
1. 解析主题参数（默认 agent-security）
2. 调用 `gh api` 搜索 GitHub 热门项目
3. 调用 WebSearch 获取相关新闻
4. 写入 `data/insights/{topic}/latest.json`
5. 复制到 `frontend/public/data/insights/`

## 5. 交互设计 (UX)

### 5.1 正常状态

```
┌─────────────────────────────────────────────────────────────┐
│ 🔭 AI 洞察                          更新于 2026-01-28 08:00  │
│ ──────────────────────────────────────────────────────────── │
│ [🛡️ Agent Security]                                         │
├─────────────────────────────────────────────────────────────┤
│ 📈 GitHub 热门项目 (7天增长)                                 │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ ⭐ +1,234  guardrails-ai/guardrails                     │ │
│ │ Adding guardrails to LLM-based applications             │ │
│ │ Python · ⭐ 5.6k                                        │ │
│ └─────────────────────────────────────────────────────────┘ │
│ 📰 行业动态                                                 │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ • Anthropic 发布 Agent 安全最佳实践    ✓ anthropic.com  │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 空状态

```
┌─────────────────────────────────────────────────────────────┐
│                    🔭 暂无洞察数据                           │
│        执行命令获取最新洞察：                                │
│        ┌────────────────────────────────┐                  │
│        │ /insight agent-security    📋  │                  │
│        └────────────────────────────────┘                  │
└─────────────────────────────────────────────────────────────┘
```

### 5.3 过期提醒

数据超过 24 小时显示：
```
⚠️ 数据已过期，执行 /insight 更新
```

## 6. 风险与应对

| 风险 | 应对措施 |
|------|----------|
| GitHub API 限流 | 使用 `gh api`（本地 token）+ 日级缓存 |
| WebSearch 结果不稳定 | 权威来源标记 ✓，手动筛选 |
| 数据文件累积 | 每主题仅保留 latest.json |
| 前端无法访问 data/ | 使用 import.meta.glob 或复制到 public/ |

## 7. 决策记录

| 决策 | 选择 | 理由 | 替代方案 |
|------|------|------|----------|
| 数据加载方式 | import.meta.glob | 与 sessions 加载保持一致 | fetch from public/ |
| 数据存储 | latest.json | 简化实现，避免累积 | 按日期归档 |
| MVP 主题 | Agent Security | 用户明确需求 | 多主题并行 |
| 空状态引导 | 显示命令 + 复制按钮 | 降低使用门槛 | 仅文字说明 |

## 8. 文件清单

| 文件 | 说明 |
|------|------|
| `data/insights/topics.json` | 主题配置 |
| `data/insights/agent-security/latest.json` | 洞察数据 |
| `frontend/src/types/insight.ts` | 类型定义 |
| `frontend/src/components/Insight/*` | 视图组件 |
| `frontend/src/services/insightService.ts` | 数据服务 |
| `.claude/commands/insight.md` | 命令定义 |
| `skills/builtin/SKILL-008-insight.json` | 技能定义 |

## 9. 可扩展性

新增主题步骤：
1. 在 `topics.json` 添加配置（设置 `enabled: true`）
2. 执行 `/insight {topic}` 生成数据
3. 前端自动渲染新主题标签
