/**
 * AthleteEvaluation - 运动员评测模块
 *
 * 展示击剑运动员数据，包括排名、趋势分析等
 */

interface AthleteEvaluationProps {
  collapsed?: boolean
}

export function AthleteEvaluation({ collapsed: _collapsed }: AthleteEvaluationProps) {
  // 左侧栏仅保留导航，内容在主区域显示
  return null
}
